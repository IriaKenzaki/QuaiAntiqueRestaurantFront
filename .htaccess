RewriteEngine On
RewriteBase /

# Prevent rewriting for 'index.html'
RewriteRule ^index\.html$ - [L]

# Conditions: If the requested file or directory does not exist


# Rewrite all other requests to 'index.html'
RewriteRule . /index.html [L]
